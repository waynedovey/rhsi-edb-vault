apiVersion: v1
kind: Service
metadata:
  name: postgres-standby
  namespace: rhsi
spec:
  selector:
    app: postgres-standby
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
