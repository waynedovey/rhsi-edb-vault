apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: rhsi-network-observer-operator
  namespace: openshift-gitops
spec:
  generators:
  - clusterDecisionResource:
      configMapRef: acm-placement
      labelSelector:
        matchLabels:
          cluster.open-cluster-management.io/placement: rhsi-network-observer-operator
      requeueAfterSeconds: 30
  template:
    metadata:
      labels:
        app.kubernetes.io/part-of: rhsi
      name: rhsi-network-observer-operator-{{name}}
    spec:
      destination:
        namespace: openshift-operators
        server: '{{server}}'
      project: default
      source:
        path: rhsi-network-observer-operator
        repoURL: https://github.com/waynedovey/rhsi-edb-vault.git
        targetRevision: main
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
